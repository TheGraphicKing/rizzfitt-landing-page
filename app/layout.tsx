import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Rizzfitt | Bangalore's Curated Pickleball Social Mixer",
  description:
    "A premium social wellness community experience at PaddleX with pickleball, a healthy breakfast buffet, fresh smoothies, and curated Bangalore connections.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.variable}>{children}</body>
    </html>
  );
}
